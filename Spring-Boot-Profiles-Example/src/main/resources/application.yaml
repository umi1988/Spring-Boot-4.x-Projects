spring:
  application:
    name: Spring-Boot-Profiles-Example
  datasource:
     driver-class-name: com.mysql.cj.jdbc.Driver  # Updated to modern driver (recommended)
     url: jdbc:mysql://localhost:3306/local_schema
     username: <db username>
     password: <db password>
  jpa:
    show-sql: true
    hibernate:
    ddl-auto: update
    properties:
          hibernate:
            dialect: org.hibernate.dialect.MySQLDialect  # Updated to modern dialect (Spring Boot 3+ / 4+)
  profiles:
     active: prod
